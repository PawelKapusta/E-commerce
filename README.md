# E-commerce
Univeristy class e-commerce course

Task 1:

Create a docker image with Debian system (without installing libraries like java, kotlin etc.) and install Kotlin,
Go or Scala. Add tools for build like sbt or gradle depends on the programming language. On this image there should be also a client to static analizing code in Kotlin... to be continued
